<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
	http://xmlns.jcp.org/xml/ns/persistence/persistence_2_1.xsd"
	version="2.1">
	<persistence-unit name="com.trybe.acc.java.hibernate.sample-hibernate">
    <!-- A unidade de persistência que recebe um nome para ser referenciada no código posteriormente. -->
	    <description>Hibernate EntityManager Sample</description>
	    <class>com.trybe.acc.java.hibernate.entity.Livro</class>
      <!-- A lists de entidades. Deve existir uma tag class para cada classe de entidade anotada como @Entity. -->
	    <exclude-unlisted-classes>true</exclude-unlisted-classes>
	    <properties>
	        <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL5Dialect"/>
	        <property name="hibernate.hbm2ddl.auto" value="update"/>
	        <property name="javax.persistence.jdbc.driver" value="com.mysql.jdbc.Driver"/>
	        <property name="javax.persistence.jdbc.url" value="jdbc:mysql://localhost:3306/library"/>
          <!-- Essa propriedade armazena a URL completa do banco MySQL. Neste caso, o MySQL está rodando na máquina local (localhost), na port default (3306), e o nome do banco de dados (schema) é library. -->
	        <property name="javax.persistence.jdbc.user" value="root"/>
          <!-- São definições de usuário e passoword para acesso ao MySQL. -->
	        <property name="javax.persistence.jdbc.password" value="root"/>
	    </properties>
	</persistence-unit>
</persistence>

<!-- O Hibernate, automaticamente, procura o arquivo src/resources/META-INF/persistence.xml quando a aplicação é iniciada, carregando suas configurações. -->